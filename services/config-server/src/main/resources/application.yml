server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Aroua-Ben-Daya/microservice.git
          clone-on-start: true
          search-paths: configs
    compatibility-verifier:
      enabled: false
